import { Character, Effect } from '@/db/character/schema';
import { SystemData } from '@/db/system/schema';

export default (character: Character, system: SystemData) => {
  const collected: Array<{ source: string; effect: Effect }> = []

  

  return collected
}