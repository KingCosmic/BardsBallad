# Refrain

Refrain is currently a lightweight page editor used for our adventurer tabs and custom modal content. Currently it's only a column layout with predefined blocks.
but in the future it may evolve into a more full-featured page editor.

which is why this is built self contained instead of following the rest of the codebase structure.